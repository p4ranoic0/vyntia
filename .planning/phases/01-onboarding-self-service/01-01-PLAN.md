---
phase: 01-onboarding-self-service
plan: "01"
type: tdd
wave: 0
depends_on: []
files_modified:
  - back/tests/conftest.py
  - back/tests/test_onboarding_service.py
  - back/tests/test_onboarding_api.py
autonomous: true
requirements:
  - ONBD-01
  - ONBD-03
  - ONBD-04
  - ONBD-05
  - ONBD-06
  - ONBD-07
  - ONBD-08

must_haves:
  truths:
    - "pytest discovers test_onboarding_service.py and test_onboarding_api.py without import errors"
    - "All tests in both files fail RED (features not yet built) — not error/skip"
    - "conftest.py provides onboarding_factory fixture usable by all test files"
  artifacts:
    - path: "back/tests/conftest.py"
      provides: "onboarding_factory, empleado_factory, usuario_factory fixtures"
      contains: "onboarding_factory"
    - path: "back/tests/test_onboarding_service.py"
      provides: "Service-level test stubs for ONBD-01, ONBD-04, ONBD-05, ONBD-06, ONBD-07"
    - path: "back/tests/test_onboarding_api.py"
      provides: "API-level test stubs for ONBD-01 corregir-correo, ONBD-03 photo, ONBD-08 list fields"
  key_links:
    - from: "back/tests/conftest.py"
      to: "back/tests/test_onboarding_service.py"
      via: "pytest fixture injection"
      pattern: "def onboarding_factory"
---

<objective>
Create the Wave 0 test scaffold for Phase 1. All downstream plans depend on these test files existing before implementation begins.

Purpose: Establish failing tests (RED) that each Wave 1+ plan will turn GREEN. Without this scaffold, plans 02-05 have no automated verify commands.
Output: conftest.py with shared fixtures, test_onboarding_service.py, test_onboarding_api.py — all discoverable, all RED.
</objective>

<execution_context>
@C:/Users/zeeke/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/zeeke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-onboarding-self-service/01-CONTEXT.md
@.planning/phases/01-onboarding-self-service/01-RESEARCH.md
@.planning/phases/01-onboarding-self-service/01-VALIDATION.md

<interfaces>
<!-- Key existing test infrastructure. Confirmed by reading back/tests/ directory. -->
<!-- No conftest.py exists yet — must be created from scratch. -->

Existing test file pattern (from test_onboarding_service.py that will be created — reference test_documentos_model.py for fixture style):
```python
# back/tests/test_documentos_model.py pattern
import pytest
from django.test import TestCase

@pytest.mark.django_db
class TestXxx:
    def test_behavior(self):
        ...
```

OnboardingEmpleado model fields (from back/app_rrhh/models/onboarding.py — confirmed via serializer):
- onboarding_id (PK)
- empleado (FK to Empleado)
- usuario (FK to Usuario)
- estado_onboarding: choices include 'pendiente_datos', 'pendiente_documentos', 'pendiente_validacion', 'observado', 'completado'
- dni_subido: BooleanField
- declaraciones_juradas_subidas: BooleanField
- certificados_academicos_subidos: BooleanField
- certificados_trabajo_subidos: BooleanField
- documentos_familiares_subidos: BooleanField
- datos_personales_completos: BooleanField
- datos_laborales_completos: BooleanField
- progreso_porcentaje: property (calculated)
- email_bienvenida_enviado: BooleanField
- fecha_email_bienvenida: DateTimeField

OnboardingService (from back/app_rrhh/services/onboarding_service.py):
- OnboardingService.crear_onboarding_completo(datos_dict) -> dict with email_enviado key
- OnboardingService.enviar_email_bienvenida(onboarding_id) -> dict
- OnboardingService.actualizar_estado_onboarding(onboarding_id) -> OnboardingEmpleado
- OnboardingService.reenviar_email_bienvenida(onboarding_id) -> dict with email_enviado key

API endpoints (existing OnboardingViewSet):
- GET /api/v1/rrhh/onboarding/ — list (paginated)
- GET /api/v1/rrhh/onboarding/mi_onboarding/ — current user's onboarding
- POST /api/v1/rrhh/onboarding/{id}/reenviar_email/ — resend email
- POST /api/v1/rrhh/onboarding/{id}/corregir-correo/ — NOT YET BUILT (Wave 1 target)

Django test settings:
- DJANGO_SETTINGS_MODULE=config.settings.testing
- Run: cd D:/INTRANET/back && D:/INTRANET/.venv/Scripts/python.exe -m pytest tests/test_onboarding_service.py tests/test_onboarding_api.py -x -v
</interfaces>
</context>

<feature>
  <name>Test scaffold for Phase 1 onboarding backend</name>
  <files>back/tests/conftest.py, back/tests/test_onboarding_service.py, back/tests/test_onboarding_api.py</files>
  <behavior>
    - conftest fixture `onboarding_factory`: creates an Empleado + Usuario + OnboardingEmpleado in the test DB, returns the OnboardingEmpleado instance. Accepts keyword overrides (e.g., estado_onboarding='completado').
    - conftest fixture `hr_client`: returns an APIClient authenticated as an RRHH user.
    - conftest fixture `onboarding_client`: returns an APIClient authenticated as the onboarding employee's usuario.
    - test_onboarding_service.py::TestActualizarEstado::test_dni_subido_updates_estado: after setting dni_subido=True and calling actualizar_estado_onboarding, the estado is not 'pendiente_datos'.
    - test_onboarding_service.py::TestActualizarEstado::test_familiar_subido: after documentos_familiares_subidos=True, actualizar_estado_onboarding reflects change.
    - test_onboarding_service.py::TestActualizarEstado::test_academico_subido: after certificados_academicos_subidos=True, actualizar_estado_onboarding reflects change.
    - test_onboarding_service.py::TestActualizarEstado::test_laboral_subido: after declaraciones_juradas_subidas=True + certificados_trabajo_subidos=True, actualizar_estado_onboarding reflects change.
    - test_onboarding_api.py::TestCorregirCorreo::test_corregir_correo_updates_email: POST /api/v1/rrhh/onboarding/{id}/corregir-correo/ with {"correo_personal": "new@test.com"} returns 2xx AND Empleado.correo_personal == "new@test.com". This test MUST fail RED since the endpoint does not yet exist.
    - test_onboarding_api.py::TestPhotoUpload::test_photo_upload_creates_documento: POST to upload a foto-type document creates a DocumentosDigitales with tipo_documento='foto'. MUST fail RED.
    - test_onboarding_api.py::TestOnboardingList::test_list_includes_last_login: GET /api/v1/rrhh/onboarding/ response items include 'last_login' field. MUST fail RED (field not in serializer yet).
  </behavior>
  <implementation>
    Step 1 (RED): Create all three files with the test stubs described above. Import the models and services correctly. Run tests — they must FAIL (not error on import, but fail on assertion). Fix any import errors until discovery succeeds with failures.

    Step 2 (no GREEN yet — GREEN happens in Wave 1 plans): Confirm pytest discovers all tests and reports failures (not errors). The RED state IS the deliverable for this Wave 0 plan.

    Key implementation notes:
    - Import path for OnboardingEmpleado: `from app_rrhh.models import OnboardingEmpleado, Empleado, Usuario`
    - Import path for OnboardingService: `from app_rrhh.services.onboarding_service import OnboardingService`
    - Import path for DocumentosDigitales: `from app_rrhh.models import DocumentosDigitales`
    - Use `@pytest.mark.django_db` on all test classes/methods that need DB access
    - For API tests, use `from rest_framework.test import APIClient`
    - For hr_client fixture: create a Usuario with tipo_usuario='rrhh' (or use the admin user pattern from test_auth_api.py)
    - For the corregir-correo test: use `hr_client.post(f'/api/v1/rrhh/onboarding/{onboarding.onboarding_id}/corregir-correo/', {'correo_personal': 'nuevo@test.com'}, format='json')` — expect 404 or 405 (will become 200 in Wave 1)
    - For photo upload test: POST to '/api/v1/rrhh/legajo/' with FormData — expect failure since endpoint currently does not enforce tipo='foto' creating ruta_fotografia side-effect
    - For last_login test: assert 'last_login' in response.data['results'][0] — will fail since field not in serializer
  </implementation>
</feature>

<verification>
```bash
cd D:/INTRANET/back && D:/INTRANET/.venv/Scripts/python.exe -m pytest tests/test_onboarding_service.py tests/test_onboarding_api.py -v --tb=short 2>&1 | tail -30
```
Expected output: All tests COLLECTED and FAILED (not ERROR). Zero import errors. The count of FAILED tests must be >= 8.
</verification>

<success_criteria>
- conftest.py exists with onboarding_factory, hr_client, onboarding_client fixtures
- test_onboarding_service.py has >= 4 test methods, all RED
- test_onboarding_api.py has >= 3 test methods, all RED
- pytest exits with code 1 (failures) not code 2 (collection error)
- No ImportError or ModuleNotFoundError in output
</success_criteria>

<output>
After completion, create `.planning/phases/01-onboarding-self-service/01-01-SUMMARY.md`
</output>
