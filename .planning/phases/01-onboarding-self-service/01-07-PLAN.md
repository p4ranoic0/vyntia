---
phase: 01-onboarding-self-service
plan: "07"
type: execute
wave: 4
depends_on:
  - "01-05"
  - "01-06"
autonomous: false
files_modified: []
requirements:
  - ONBD-01
  - ONBD-02
  - ONBD-03
  - ONBD-04
  - ONBD-05
  - ONBD-06
  - ONBD-07
  - ONBD-08

must_haves:
  truths:
    - "Employee with active onboarding cannot access the admin panel or other routes"
    - "Employee can upload a photo and all document types and see status badges update"
    - "Progress bar updates immediately after each upload"
    - "RRHH can monitor all onboardings with correct completion percentages"
    - "RRHH can correct a wrong email and resend the welcome email"
    - "5-day no-response alert appears in the Alerta column for qualifying employees"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for the complete Phase 1 onboarding self-service feature. All automated work is done — this plan asks RRHH (the user) to walk through the full flow and confirm everything works end-to-end.

Purpose: Catch any visual, UX, or functional issues that automated tests cannot detect — wrong labels, missing states, confusing flows, incorrect behavior in real browser context.
Output: User confirmation that Phase 1 is complete and all 8 requirements are satisfied.
</objective>

<execution_context>
@C:/Users/zeeke/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/zeeke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-onboarding-self-service/01-CONTEXT.md
@.planning/phases/01-onboarding-self-service/01-05-SUMMARY.md
@.planning/phases/01-onboarding-self-service/01-06-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final automated test suite run</name>
  <files>N/A — validation runner, no files modified</files>
  <action>
    Run the full backend and frontend test suites and confirm all green before handing off to human verification.

    ```bash
    cd D:/INTRANET/back && D:/INTRANET/.venv/Scripts/python.exe -m pytest tests/test_onboarding_service.py tests/test_onboarding_api.py -v
    ```

    ```bash
    cd D:/INTRANET/back && make test
    ```

    ```bash
    cd D:/INTRANET/front && npm run test
    ```

    Start the dev server:
    ```bash
    cd D:/INTRANET/back && D:/INTRANET/.venv/Scripts/python.exe manage.py runserver 8000 --settings=config.settings.development
    cd D:/INTRANET/front && npm run dev
    ```

    Fix any test failures or build errors before proceeding to the human verification checkpoint.
  </action>
  <verify>
    <automated>cd D:/INTRANET/back && D:/INTRANET/.venv/Scripts/python.exe -m pytest tests/test_onboarding_service.py tests/test_onboarding_api.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>All onboarding backend tests GREEN. Frontend build passes. Dev server starts without errors.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Complete Phase 1 Onboarding Self-Service:
    - Backend: serializer updated (last_login, fecha_nacimiento in EmpleadoUpdateSerializer), corregir-correo action, subir-foto action, subir-documento action
    - Frontend: OnboardingRoute guard (redirects employees to /onboarding), tabbed OnboardingEmployeePage with 4 tabs (Personal | Familiar | Académico | Laboral), DocumentUploadZone with drag-and-drop + 10 MB limit, progress bar above tabs, OnboardingCompleteBanner, enhanced RRHH admin panel with progress bars + Alerta column + email correction flow
  </what-built>
  <how-to-verify>
    Verify as EMPLOYEE (log in with an onboarding employee account):

    1. Navigate to http://localhost:5173 → confirm redirect to /onboarding (not the admin panel)
    2. Confirm progress bar is visible above 4 tabs: Personal, Familiar, Académico, Laboral
    3. Tab Personal: confirm Avatar at top + editable fields (Teléfono celular, Dirección domicilio, Fecha de nacimiento) + DNI upload zone + Carné de extranjería zone
    4. Upload a JPG profile photo → confirm Avatar updates, zone shows "Pendiente revisión" badge, progress bar increases
    5. Upload a PDF for DNI → confirm zone shows filename + date + "Pendiente revisión" badge
    6. Fill in and save personal fields → confirm no API error (check network tab: request body uses telefono_celular, direccion_domicilio, fecha_nacimiento)
    7. Tab Laboral: confirm cargo/area/regimen fields are read-only (labeled "Datos gestionados por RRHH"), document upload zones are interactive
    8. Try navigating directly to /admin → confirm redirect back to /onboarding

    Verify as RRHH (log in with an RRHH account):

    9. Navigate to /onboarding/admin → confirm table with columns: Empleado, Estado, Progreso (%), Fecha inicio, Alerta, Acciones
    10. Confirm Progreso column shows a progress bar + percentage value
    11. Create a new onboarding for a test employee with a deliberately wrong email → confirm amber alert banner appears with "Reintentar enviar" and "Corregir correo y reintentar" buttons
    12. Click "Corregir correo y reintentar" → enter correct email → confirm success toast
    13. Filter table by "Con alertas" → confirm only rows where last_login is null AND 5+ days since email appear
    14. Click "Rechazar" on a row → confirm Dialog appears (NOT a browser prompt()) with textarea for observations
    15. Confirm RRHH user is NOT redirected to /onboarding when navigating normally
  </how-to-verify>
  <resume-signal>Type "aprobado" if all 15 steps pass. Describe any issues found (step number + what went wrong).</resume-signal>
</task>

</tasks>

<verification>
Human verification covering all 8 ONBD requirements. All steps listed in the checkpoint must pass.
</verification>

<success_criteria>
- Steps 1-8 (employee flow) all pass
- Steps 9-15 (RRHH flow) all pass
- No visual regressions in other pages
- User types "aprobado"
</success_criteria>

<output>
After completion, create `.planning/phases/01-onboarding-self-service/01-07-SUMMARY.md`

Then update ROADMAP.md Phase 1 Plans section to show all plans as [x] complete.
</output>
