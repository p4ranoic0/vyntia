---
phase: 01-onboarding-self-service
plan: "13"
type: execute
wave: 8
depends_on:
  - "01-12"
autonomous: false
files_modified: []
requirements:
  - ONBD-09
  - ONBD-10
  - ONBD-11
  - ONBD-12
  - ONBD-13
  - ONBD-14
  - ONBD-15

must_haves:
  truths:
    - "Employee can register N family dependents with required docs per parentesco"
    - "Employee can register N academic records across three categories (certificados, cursos, titulos)"
    - "Employee can register N work history entries"
    - "Document preview modal opens before every upload — employee must confirm before POST"
    - "Rejected documents show reason and Corregir y reenviar button"
    - "Progress bar shows both upload % and approved %"
    - "Toast notifications fire for every action success and error"
    - "RRHH can approve or reject documents individually in the admin panel"
    - "Employee receives email when a document is rejected individually"
    - "Employee receives email when full onboarding is approved or observed"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for the expanded Phase 1 features (plans 01-08 through 01-12). All automated work is complete — this plan asks the tester to walk through the full expanded onboarding flow end-to-end and confirm every new capability works correctly in a running development environment.

Purpose: Catch any visual, UX, or functional issues that automated tests cannot detect — wrong labels, missing states, broken preview modal, incorrect email delivery, incomplete N-item lists, or regressions in the baseline Waves 0-4 features.
Output: User confirmation that all 16 success criteria are satisfied and expanded Phase 1 is complete.
</objective>

<execution_context>
@C:/Users/zeeke/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/zeeke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-onboarding-self-service/01-CONTEXT.md
@.planning/phases/01-onboarding-self-service/01-12-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final automated test suite run and migration verification</name>
  <files>N/A — validation runner, no files modified</files>
  <action>
    Run migrations to confirm DB is current:
    ```bash
    cd D:/INTRANET/back && D:/INTRANET/.venv/Scripts/python.exe manage.py migrate --settings=config.settings.development 2>&1 | tail -10
    ```

    Run backend onboarding tests:
    ```bash
    cd D:/INTRANET/back && D:/INTRANET/.venv/Scripts/python.exe -m pytest tests/test_onboarding_service.py tests/test_onboarding_api.py -v --tb=short 2>&1 | tail -30
    ```

    Run full backend test suite:
    ```bash
    cd D:/INTRANET/back && make test
    ```

    Run frontend build:
    ```bash
    cd D:/INTRANET/front && npm run build
    ```

    Start dev servers (run each in a separate terminal):
    ```bash
    cd D:/INTRANET/back && D:/INTRANET/.venv/Scripts/python.exe manage.py runserver 8000 --settings=config.settings.development
    cd D:/INTRANET/front && npm run dev
    ```

    Fix any test failures or build errors before handing off to the human checkpoint.
    If backend uses console EMAIL_BACKEND in development, emails will appear in the runserver terminal — this is acceptable for verification purposes.
  </action>
  <verify>
    <automated>cd D:/INTRANET/back && D:/INTRANET/.venv/Scripts/python.exe -m pytest tests/test_onboarding_service.py tests/test_onboarding_api.py -v --tb=short 2>&1 | tail -20</automated>
  </verify>
  <done>All backend onboarding tests GREEN. Frontend build passes with 0 TypeScript errors. Migration 0025 applied. Dev servers running at localhost:8000 and localhost:5173.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: End-to-end expanded onboarding verification (16 steps)</name>
  <files>N/A — human verification, no files modified</files>
  <action>Human tester follows all 16 steps in how-to-verify to confirm every expanded feature works end-to-end.</action>
  <verify>Human confirms all 16 steps pass and types "aprobado".</verify>
  <done>All 16 steps confirmed. No regressions. User types "aprobado".</done>
  <what-built>
    Expanded Phase 1 Onboarding Self-Service (plans 01-08 through 01-12):
    - Backend: CursosCertificaciones model + migration 0025, familiar FK on DocumentosDigitales, documento FK on DatosAcademicos, progreso_aprobado property on OnboardingEmpleado
    - Backend: DatosFamiliaresViewSet + DatosAcademicosViewSet opened to employee write (ownership-scoped), CursosCertificacionesViewSet (new), aprobar_documento + rechazar_documento actions on OnboardingViewSet, subir-documento extended with familiar_id/academico_id/curso_id linking
    - Backend: OnboardingNotificationService with 3 email notification methods + 6 email templates, wired into validar and rechazar_documento actions
    - Frontend: accordion.tsx component, onboardingDataService.ts with CRUD functions for familiares/academicos/cursos
    - Frontend: OnboardingTabFamiliar rewritten with N-item dependents + per-parentesco document zones
    - Frontend: OnboardingTabAcademico rewritten with 3-section accordion (certificados, cursos, titulos)
    - Frontend: OnboardingTabLaboral with N-item experiencia laboral section
    - Frontend: DocumentPreviewModal component (preview before upload — Cancelar / Enviar documento)
    - Frontend: DocumentUploadZone updated — preview-before-upload, Corregir y reenviar for rejected docs, comprehensive toasts
    - Frontend: OnboardingProgressBar updated to show upload% AND approved%
    - Frontend: OnboardingAdminPage — per-document Aprobar/Rechazar panel with Dialog for motivo
  </what-built>
  <how-to-verify>
    Start both dev servers before testing. Open http://localhost:5173

    === EMPLOYEE FLOW (log in as an onboarding employee) ===

    Step 1 — Familiar tab — Add dependent:
    - Go to Tab "Familiar" → click "Agregar dependiente"
    - Fill: nombres, apellidos, parentesco=hijo, fecha_nacimiento, numero_documento → save
    - Confirm new familiar card appears in the list with the dependent's data
    - Confirm toast "Datos guardados correctamente" appears

    Step 2 — Required docs per parentesco:
    - For the hijo card: confirm TWO upload zones appear ("DNI del familiar" + "Partida de nacimiento")
    - Add a second dependent with parentesco=conyuge → confirm zones change to "DNI del familiar" + "Acta de matrimonio / Cert. union de hecho"

    Step 3 — Document upload preview (PDF):
    - Drop a PDF onto the "DNI del familiar" zone
    - Confirm preview modal opens BEFORE the file is uploaded (no immediate API call)
    - Confirm modal shows iframe with PDF content + filename + file size
    - Click "Cancelar" → confirm modal closes, zone returns to empty state (no upload occurred)
    - Drop the same PDF again → click "Enviar documento" → confirm upload completes
    - Confirm zone shows "Pendiente revision" amber badge
    - Confirm green toast "Documento enviado correctamente" appears

    Step 4 — Academico tab — Three accordion sections:
    - Tab "Academico" → confirm three accordion sections: "Certificados de Estudio", "Cursos y Diplomados", "Titulos Profesionales"
    - In "Cursos y Diplomados" → click "Agregar curso" → fill nombre_curso, institucion, fecha_inicio, horas → save
    - Confirm new curso card appears with a DocumentUploadZone for its certificate PDF
    - In "Titulos Profesionales" → click "Agregar titulo" → fill carrera, institucion, tipo=bachiller → save
    - Confirm new titulo card appears with a DocumentUploadZone
    - Upload a PDF to the titulo zone → preview modal opens → confirm → "Pendiente revision" badge appears

    Step 5 — Laboral tab — Work history:
    - Tab "Laboral" → confirm "Experiencia Laboral" section is present
    - Click "Agregar certificado de trabajo" → fill empresa, fecha_inicio, fecha_fin → save
    - Confirm new work entry card appears with a DocumentUploadZone for constancia de trabajo
    - Confirm RRHH-managed fields (cargo, area, regimen) remain read-only / labeled "Datos gestionados por RRHH"
    - Confirm DDJJ, CV, carta de recomendacion static upload zones are still present

    Step 6 — Progress bar dual values:
    - Confirm progress bar shows two labeled values: "X% subido" and "Y% aprobado"
    - Upload % should reflect the documents you uploaded; approved % should still be 0 at this point

    Step 7 — Toast error scenarios:
    - Drop a file larger than 10MB → confirm red toast "El archivo supera el limite de 10MB"
    - Drop a non-PDF file onto a PDF zone → confirm red toast "Formato no valido — solo PDF"
    - (Optional) Disconnect network and attempt upload → confirm "Sin conexion. Verifica tu internet."

    Step 8 — Image file preview (Personal tab):
    - On Tab "Personal", drop a JPG file onto the profile photo zone
    - Confirm preview modal shows an image preview (not an iframe)
    - Click "Enviar documento" → confirm upload completes → Avatar updates

    === RRHH FLOW (log in as RRHH) ===

    Step 9 — Per-document approval panel:
    - Navigate to /onboarding/admin → open detail for the employee used in steps 1-8
    - Confirm "Documentos subidos" section shows the list of all uploaded docs
    - Confirm each row shows: document name, estado badge (amber "Pendiente revision"), "Ver" link, "Aprobar" button, "Rechazar" button

    Step 10 — Approve a document:
    - Click "Aprobar" on the DNI familiar document
    - Confirm green toast "Documento aprobado" appears
    - Confirm document badge changes to "Aprobado" (green)
    - Switch to employee view → confirm approved % in the progress bar is now > 0

    Step 11 — Reject a document with motivo:
    - Click "Rechazar" on a second document
    - Confirm a Dialog opens (NOT a browser prompt())
    - Enter motivo: "El documento es ilegible" → click "Confirmar rechazo"
    - Confirm success toast appears ("Documento rechazado y notificacion enviada al empleado")
    - Switch to employee view → confirm that document zone shows:
      - Red "Rechazado" badge
      - "El documento es ilegible" text below the badge
      - "Corregir y reenviar" button

    Step 12 — Corregir y reenviar flow:
    - As employee, click "Corregir y reenviar" on the rejected document
    - Confirm zone resets to show the dropzone (active, accepts new files)
    - Drop a new corrected PDF → preview modal opens → click "Enviar documento"
    - Confirm zone returns to "Pendiente revision" state

    Step 13 — Email notification for document rejection (ONBD-14):
    - Check the Django runserver terminal after step 11
    - Confirm output includes an email with subject "Documento rechazado: [nombre_documento]" and the motivo "El documento es ilegible"
    - (If using SMTP: check the employee email inbox directly)

    Step 14 — Onboarding approval email (ONBD-14):
    - As RRHH, click "Validar/Aprobar" onboarding for the employee
    - Confirm onboarding estado changes to "completado"
    - Confirm email "¡Tu onboarding ha sido aprobado!" appears in the Django terminal (or inbox)

    Step 15 — Onboarding observed email (ONBD-14):
    - Create or use a second test employee in onboarding
    - As RRHH, mark their onboarding as "Observado" with observaciones text
    - Confirm email "Tu onboarding requiere correcciones" appears in the Django terminal with the observation text

    Step 16 — No regressions from plans 01-01 through 01-07:
    - Verify Tab "Personal" still shows Avatar + personal data fields + DNI upload zone
    - Verify progress bar is still displayed above the 4 tabs
    - Verify RRHH admin table still has Progreso column (progress bars) and Alerta column (5-day warning)
    - Verify creating a new onboarding with an invalid email still shows the alert banner with "Corregir correo" button
  </how-to-verify>
  <resume-signal>Type "aprobado" if all 16 steps pass. Describe any issues found with the step number and what went wrong.</resume-signal>
</task>

</tasks>

<verification>
Human verification covering the 7 new ONBD requirements (ONBD-09 through ONBD-15) across the expanded Phase 1. All 16 steps listed in the checkpoint must pass. Step 16 confirms no regressions to baseline Waves 0-4 features.
</verification>

<success_criteria>
- Steps 1-8 (employee expanded N-item flow + preview modal + toasts) all pass
- Steps 9-12 (RRHH per-document approval + Corregir y reenviar cycle) all pass
- Steps 13-15 (email notifications for rejection, approval, and observed) all pass
- Step 16 (no regressions to baseline features from plans 01-01 through 01-07) passes
- User types "aprobado"
</success_criteria>

<output>
After completion, create `.planning/phases/01-onboarding-self-service/01-13-SUMMARY.md`

Then update ROADMAP.md Phase 1 Plans section to mark plans 01-08 through 01-13 complete.
Update STATE.md stopped_at to reflect Phase 1 fully complete.
</output>
