---
phase: 01-onboarding-self-service
plan: "13"
type: execute
wave: 8
depends_on:
  - "01-12"
autonomous: false
files_modified: []
requirements:
  - ONBD-01
  - ONBD-02
  - ONBD-03
  - ONBD-04
  - ONBD-05
  - ONBD-06
  - ONBD-07
  - ONBD-08
  - ONBD-09
  - ONBD-10
  - ONBD-11
  - ONBD-12
  - ONBD-13
  - ONBD-14
  - ONBD-15

must_haves:
  truths:
    - "Employee can register N family dependents with required docs per parentesco"
    - "Employee can register N academic records across three categories"
    - "Employee can register N work history entries"
    - "Document preview modal opens before every upload — employee must confirm before POST"
    - "Rejected documents show reason and Corregir y reenviar button"
    - "Progress bar shows both upload % and approved %"
    - "Toast notifications fire for every action success and error"
    - "RRHH can approve or reject documents individually in the admin panel"
    - "RRHH receives email notification when onboarding is approved or rejected"
    - "Employee receives email when a document is rejected individually"
  artifacts: []
  key_links: []
---

<objective>
Human verification checkpoint for all expanded Phase 1 features (plans 01-08 through 01-12). All automated work is complete — this plan asks RRHH (the user) to walk through the full expanded onboarding flow and confirm every new capability works correctly.

Purpose: Catch any visual, UX, or functional regressions that automated tests cannot detect — wrong labels, missing states, broken preview modal, incorrect email delivery, incomplete N-item lists.
Output: User confirmation that all 16 success criteria are satisfied and expanded Phase 1 is complete.
</objective>

<execution_context>
@C:/Users/zeeke/.claude/get-shit-done/workflows/execute-plan.md
@C:/Users/zeeke/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-onboarding-self-service/01-CONTEXT.md
@.planning/phases/01-onboarding-self-service/01-12-SUMMARY.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Final automated test suite run and migration verification</name>
  <files>N/A — validation runner, no files modified</files>
  <action>
    Run migrations to confirm DB is current:
    ```bash
    cd D:/INTRANET/back && D:/INTRANET/.venv/Scripts/python.exe manage.py migrate --settings=config.settings.development 2>&1 | tail -10
    ```

    Run backend tests:
    ```bash
    cd D:/INTRANET/back && D:/INTRANET/.venv/Scripts/python.exe -m pytest tests/test_onboarding_service.py tests/test_onboarding_api.py -v
    ```

    Run full backend test suite:
    ```bash
    cd D:/INTRANET/back && make test
    ```

    Run frontend build and tests:
    ```bash
    cd D:/INTRANET/front && npm run build && npm run test
    ```

    Start servers:
    ```bash
    cd D:/INTRANET/back && D:/INTRANET/.venv/Scripts/python.exe manage.py runserver 8000 --settings=config.settings.development
    cd D:/INTRANET/front && npm run dev
    ```

    Fix any test failures or build errors before handing off to the human checkpoint.
  </action>
  <verify>
    <automated>cd D:/INTRANET/back && D:/INTRANET/.venv/Scripts/python.exe -m pytest tests/test_onboarding_service.py tests/test_onboarding_api.py -v 2>&1 | tail -20</automated>
  </verify>
  <done>All backend onboarding tests GREEN. Frontend build passes. Migration 0025 applied. Dev servers running.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
    Expanded Phase 1 Onboarding Self-Service (plans 01-08 through 01-12):
    - Backend: CursosCertificaciones model + migration 0025, familiar FK on DocumentosDigitales, documento FK on DatosAcademicos, progreso_aprobado property on OnboardingEmpleado
    - Backend: DatosFamiliaresViewSet + DatosAcademicosViewSet opened to employee write (ownership-scoped), CursosCertificacionesViewSet (new), aprobar_documento + rechazar_documento actions on OnboardingViewSet, subir-documento extended with familiar_id/academico_id/curso_id linking
    - Backend: OnboardingNotificationService with 3 email notification methods, 6 new email templates, wired into validar action
    - Frontend: accordion.tsx component, onboardingDataService.ts with 11 CRUD functions
    - Frontend: OnboardingTabFamiliar rewritten with N-item dependents + per-parentesco document zones
    - Frontend: OnboardingTabAcademico rewritten with 3-section accordion (certificados, cursos, titulos)
    - Frontend: OnboardingTabLaboral with N-item experiencia laboral section
    - Frontend: DocumentPreviewModal component (preview before upload with Cancelar / Enviar documento)
    - Frontend: DocumentUploadZone updated — preview-before-upload, Corregir y reenviar for rejected docs, comprehensive toasts
    - Frontend: OnboardingProgressBar updated to show upload% AND approved%
    - Frontend: OnboardingAdminPage — per-document Aprobar/Rechazar panel with Dialog for motivo
  </what-built>
  <how-to-verify>
    Start both servers before testing. Log in at http://localhost:5173

    === EMPLOYEE FLOW (log in as an onboarding employee) ===

    Step 1 — Familiar tab — Add dependent:
    - Go to Tab "Familiar" → click "Agregar dependiente"
    - Fill: nombres, apellidos, parentesco=hijo, fecha_nacimiento, DNI → save
    - Confirm new familiar card appears in the list

    Step 2 — Familiar tab — Required docs per parentesco:
    - For the hijo card: confirm TWO upload zones appear ("DNI del familiar" + "Partida de nacimiento")
    - Add a second dependent with parentesco=conyuge → confirm zones are "DNI del familiar" + "Acta de matrimonio / Cert. union de hecho"

    Step 3 — Familiar tab — Document upload preview:
    - Drop a PDF onto the "DNI del familiar" zone
    - Confirm preview modal opens BEFORE the file is uploaded (no immediate API call)
    - Confirm modal shows iframe with PDF content + filename + file size
    - Click "Cancelar" → confirm modal closes, zone returns to empty state (no upload occurred)
    - Drop the same PDF again → click "Enviar documento" → confirm upload completes, zone shows "Pendiente revision" badge
    - Confirm green toast "Documento enviado correctamente" appears

    Step 4 — Academico tab — Three accordion sections:
    - Tab "Academico" → confirm three accordion sections: "Certificados de Estudio", "Cursos y Diplomados", "Titulos Profesionales"
    - In "Cursos y Diplomados" → click "Agregar curso" → fill nombre_curso, institucion, fecha_inicio, horas → save
    - Confirm new curso card appears with a DocumentUploadZone for its PDF
    - In "Titulos Profesionales" → click "Agregar titulo" → fill carrera, institucion, tipo=bachiller → save

    Step 5 — Laboral tab — Work history:
    - Tab "Laboral" → confirm "Experiencia Laboral" accordion section
    - Click "Agregar certificado de trabajo" → fill empresa, fecha_inicio, fecha_fin → save
    - Confirm new work entry card appears with a DocumentUploadZone for constancia de trabajo
    - Confirm RRHH-managed fields (cargo, area, regimen) are still read-only
    - Confirm DDJJ, CV, carta de recomendacion static upload zones are still present

    Step 6 — Progress bar dual values:
    - Confirm progress bar shows two values: "X% subido — Y% aprobado"
    - The aprobado% should be 0 until RRHH approves documents (step 10 below)

    Step 7 — Toast error scenarios:
    - Drop a file larger than 10MB → confirm red toast "El archivo supera el limite de 10MB"
    - Drop a non-PDF file on a PDF zone → confirm red toast "Formato no valido — solo PDF"
    - (Optional) Turn off the network and try to upload → confirm "Sin conexion. Verifica tu internet."

    Step 8 — Image file preview:
    - On Tab "Personal", drop a JPG profile photo → confirm preview modal shows <img> (not iframe)
    - Confirm → upload completes → Avatar updates

    === RRHH FLOW (log in as RRHH) ===

    Step 9 — Per-document approval panel:
    - Navigate to /onboarding/admin → expand or open detail for the employee from step 3
    - Confirm "Documentos subidos" section shows the list of uploaded docs with estado badges
    - Confirm each doc row has "Ver" link (opens PDF), "Aprobar" button, "Rechazar" button

    Step 10 — Approve a document:
    - Click "Aprobar" on the DNI familiar document → confirm green toast "Documento aprobado"
    - Document badge changes to "Aprobado"
    - Switch to employee view — confirm progress bar approved% has increased

    Step 11 — Reject a document with motivo:
    - Click "Rechazar" on another document → confirm Dialog opens (NOT browser prompt())
    - Enter motivo: "El documento es ilegible" → confirm "Confirmar rechazo"
    - Confirm amber/green toast "Documento rechazado y notificacion enviada al empleado"
    - Switch to employee view — rejected document zone shows red "Rechazado" badge + "El documento es ilegible" text + "Corregir y reenviar" button

    Step 12 — Corregir y reenviar flow:
    - As employee, click "Corregir y reenviar" on the rejected document
    - Confirm zone resets to allow new upload (dropzone becomes active again)
    - Drop a new corrected PDF → preview modal opens → confirm upload
    - Confirm document status returns to "Pendiente revision"

    Step 13 — Email notification for document rejection:
    - Check the email sent to the employee when document was rejected in Step 11
    - Confirm email arrived with subject "Documento rechazado: [nombre]" and motivo visible
    - (Use console EMAIL_BACKEND if SMTP not configured — check Django dev server logs for email output)

    Step 14 — Onboarding approve + email:
    - As RRHH, click "Validar/Aprobar" onboarding for the employee
    - Confirm onboarding estado changes to "completado"
    - Confirm email "¡Tu onboarding ha sido aprobado!" sent to employee (check server logs)

    Step 15 — Onboarding reject/observe + email:
    - As RRHH, reject a different employee's onboarding with observaciones
    - Confirm email "Tu onboarding requiere correcciones" sent with the observation text

    Step 16 — No regressions from plans 01-01 through 01-07:
    - Verify Tab "Personal" still shows Avatar + personal fields + DNI upload zone (unchanged)
    - Verify progress bar still shows at the top above the 4 tabs
    - Verify RRHH admin table still has Progreso column with progress bars + Alerta column
    - Verify corregir-correo flow still works (create onboarding with bad email → alert banner → correct email)
  </how-to-verify>
  <resume-signal>Type "aprobado" if all 16 steps pass. Describe any issues found (step number + what went wrong).</resume-signal>
</task>

</tasks>

<verification>
Human verification covering all 16 ONBD requirements across expanded Phase 1. All 16 steps listed in the checkpoint must pass without regressions to plans 01-01 through 01-07.
</verification>

<success_criteria>
- Steps 1-8 (employee expanded flow) all pass
- Steps 9-15 (RRHH approval + email flow) all pass
- Step 16 (no regressions) passes
- User types "aprobado"
</success_criteria>

<output>
After completion, create `.planning/phases/01-onboarding-self-service/01-13-SUMMARY.md`

Then update ROADMAP.md Phase 1 Plans section to mark plans 01-08 through 01-13 complete.
Update STATE.md stopped_at to reflect Phase 1 fully complete.
</output>
