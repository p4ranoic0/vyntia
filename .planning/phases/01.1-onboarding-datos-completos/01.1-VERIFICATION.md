---
phase: 01.1-onboarding-datos-completos
verified: 2026-03-15T14:40:00Z
status: passed
score: 6/6 gaps closed
re_verification:
  previous_status: gaps_found
  previous_score: 9/9 truths verified (code present but runtime bugs found in UAT)
  gaps_closed:
    - "GAP-01: Routing guard — completed employees navigating to /onboarding are now redirected to /"
    - "GAP-02: archivo_url — DocumentosDigitalesSerializer now returns absolute URL; subir-documento response includes archivo_url"
    - "GAP-03: Toast feedback — DocumentUploadZone already had toast.success; legajo-docs invalidation now triggers UI refresh confirming upload"
    - "GAP-04: Stale UI — all tab components invalidate ['legajo-docs', empleadoId] on create/delete/upload; DocumentUploadZone invalidates ['legajo-docs'] broadly"
    - "GAP-05: Permissions — es_version_actual filter added; DocumentosDigitalesViewSet.get_queryset correctly scopes to employee's own documents"
    - "GAP-06: Test coverage — 4 backend tests (test_documentos_digitales_onboarding.py) and 7 frontend tests (OnboardingSectionStatus.test.tsx + onboardingUploadService.test.ts) added; all pass"
  gaps_remaining: []
  regressions: []
---

# Phase 01.1: Onboarding Datos Completos — Re-Verification Report

**Phase Goal:** Close all UAT gaps found in Phase 1 verification — fix archivo_url backend, frontend routing guard, document API URL, stale UI invalidation, and add test coverage.
**Verified:** 2026-03-15T14:40:00Z
**Status:** PASSED
**Re-verification:** Yes — after gap closure via plans 01.1-05, 01.1-06, 01.1-07

---

## Gap Closure Summary

The previous verification (2026-03-15T20:00:00Z) found 6 runtime gaps after UAT. All 6 were addressed by three gap-closure plans and are now confirmed closed by direct codebase inspection and live test runs.

---

## Gap-by-Gap Verification

### GAP-01: Routing guard — completed employees redirect from /onboarding to /

**Previous status:** FAILED — no guard checked `estado_onboarding === 'completado'`

**Evidence of closure:**

`front/src/App.tsx` now contains `OnboardingGuard` component (line 102) with the following logic:
- Fetches `['mi-onboarding']` query (shared with `OnboardingRoute`, zero extra network requests)
- If `onboarding?.estado_onboarding === 'completado'` → returns `<Navigate to="/" replace />`
- The `/onboarding` route is wrapped: `<OnboardingGuard><OnboardingPage /></OnboardingGuard>`

The `OnboardingRoute` (wrapping dashboard) redirects in-progress employees TO `/onboarding`. The new `OnboardingGuard` (wrapping the `/onboarding` route itself) redirects completed employees AWAY. The two guards are complementary and correctly placed.

**Status: CLOSED**

---

### GAP-02: archivo_url — employee can view uploaded documents

**Previous status:** FAILED — `DocumentosDigitalesSerializer` had no `archivo_url` field; the "Ver documento" button guard `existingDoc.archivo_url` was always falsy

**Evidence of closure:**

`back/api/v1/rrhh/serializers.py` (line 1077):
- `archivo_url = serializers.SerializerMethodField()` added to `DocumentosDigitalesSerializer`
- `get_archivo_url()` method returns `request.build_absolute_uri(obj.archivo.url)` with fallback to relative URL
- `"archivo_url"` added to `Meta.fields` list after `"archivo"`

`back/api/v1/rrhh/views.py`:
- `subir_documento` response (line 2579): `"archivo_url": request.build_absolute_uri(doc.archivo.url) if doc.archivo else None`
- `subir_foto` response (line 2471): same field present

Live backend test confirms field is present in API response:
- `test_response_includes_archivo_url_field` PASSED (4/4 backend tests in `test_documentos_digitales_onboarding.py`)

**Status: CLOSED**

---

### GAP-03: Toast feedback after successful upload

**Previous status:** FAILED — "only error handling, no success toast"

**Evidence of closure:**

`front/src/features/onboarding/components/DocumentUploadZone.tsx` line 42 already had `toast.success('Documento enviado correctamente')`. The root cause of the "no feedback" symptom was the stale UI (GAP-04) — the document list didn't update, making the upload appear to fail. With GAP-04 fixed (legajo-docs invalidation), the document card now refreshes immediately after the toast fires, completing the feedback loop.

Line 44 now also added: `queryClient.invalidateQueries({ queryKey: ['legajo-docs'] })`

**Status: CLOSED**

---

### GAP-04: Stale UI after saving data

**Previous status:** FAILED — `invalidateQueries` called `['mi-onboarding']` only; the `['legajo-docs', empleadoId]` query (which drives the document section panel) was never invalidated

**Evidence of closure:**

All mutation `onSuccess` handlers now invalidate `['legajo-docs', empleadoId]`:

| Component | Locations |
|-----------|-----------|
| `OnboardingTabFamiliar.tsx` | createMutation.onSuccess (line 153), deleteMutation.onSuccess (line 178), handleUploadSuccess (line 236) |
| `OnboardingTabAcademico.tsx` | certMutation success (line 167), handleUploadSuccess (line 244) |
| `OnboardingTabLaboral.tsx` | handleSubmit success (line 113), inline delete handler (line 238) |
| `DocumentUploadZone.tsx` | handleUpload success (line 44) — broad `['legajo-docs']` without id |

The `OnboardingEmployeePage.tsx` fetches with `queryKey: ['legajo-docs', empleadoId]` — the invalidation keys match precisely. React Query will refetch on next render.

**Status: CLOSED**

---

### GAP-05: Permission issues — es_version_actual filter

**Previous status:** FAILED — "possible 403 errors in onboarding actions for employee role"

**Evidence of closure:**

`back/api/v1/rrhh/views.py` `DocumentosDigitalesViewSet.get_queryset` (lines 1957-1961):

```python
es_version_actual = self.request.query_params.get("es_version_actual")
if es_version_actual == "true":
    queryset = queryset.filter(es_version_actual=True)
elif es_version_actual == "false":
    queryset = queryset.filter(es_version_actual=False)
```

The non-HR user permission scope (line 1947: `queryset = queryset.filter(empleado=user.empleado)`) was already present in the codebase. The new `es_version_actual` filter is added after this scope — so a non-HR employee can only see their own documents, and can further filter by version.

`test_employee_cannot_see_other_employee_documents` PASSED — cross-employee isolation confirmed.
`test_es_version_actual_filter` PASSED — old versions excluded when param is `true`.

**Status: CLOSED**

---

### GAP-06: Test coverage for plans 01.1-02 through 01.1-04

**Previous status:** FAILED — no frontend or additional backend tests existed for the onboarding components

**Evidence of closure:**

**Backend tests** — `back/tests/test_documentos_digitales_onboarding.py` (4 tests, all PASS):
- `test_employee_can_list_own_documents` — 200 OK for own employee
- `test_response_includes_archivo_url_field` — `archivo_url` key present in every document
- `test_employee_cannot_see_other_employee_documents` — cross-employee isolation
- `test_es_version_actual_filter` — old versions excluded

**Frontend tests** — 7 tests across 2 files, all PASS:

`OnboardingSectionStatus.test.tsx` (6 tests via `computeStatus` unit tests):
- `pendiente` when no docs
- `pendiente` when docs don't match section tipos
- `observado` when any rechazado
- `completo` when all aprobado
- `en_revision` when mix of aprobado + pendiente_revision
- Render test: 4 section labels visible (Personal, Familiar, Academico, Laboral)

`onboardingUploadService.test.ts` (1 test):
- `subirDocumento` calls `POST /api/v1/rrhh/onboarding/subir-documento/` with FormData

Live test run output confirms: `2 passed (7 tests)` for the two new frontend test files. The 1 failed suite (`tests/e2e/auth.test.js`) is the pre-existing Playwright file incorrectly included in Vitest's glob — not a regression introduced by this phase.

**Status: CLOSED**

---

## Observable Truths (Re-verification)

| # | Truth | Status | Evidence |
|---|-------|--------|----------|
| 1 | Completed employees navigating to /onboarding are redirected to / | VERIFIED | `OnboardingGuard` in `App.tsx` line 102–123; `estado_onboarding === 'completado'` → `Navigate to="/"` |
| 2 | GET /api/v1/rrhh/documentos-digitales/ returns documents with archivo_url populated | VERIFIED | `DocumentosDigitalesSerializer` has `archivo_url = SerializerMethodField()`; `get_archivo_url` uses `build_absolute_uri`; backend test PASSES |
| 3 | POST /api/v1/rrhh/onboarding/subir-documento/ response includes archivo_url | VERIFIED | `views.py` line 2579: `"archivo_url": request.build_absolute_uri(doc.archivo.url) if doc.archivo else None` |
| 4 | OnboardingEmployeePage fetches from /api/v1/rrhh/documentos-digitales/ | VERIFIED | `OnboardingEmployeePage.tsx` line 27; `onboardingDataService.ts` `getConstanciasTrabajo` line 64 — both use correct URL |
| 5 | Document list updates immediately after upload without manual refresh | VERIFIED | All 6 mutation sites now call `invalidateQueries(['legajo-docs', empleadoId])`; `DocumentUploadZone` calls `invalidateQueries(['legajo-docs'])` |
| 6 | es_version_actual=true filter excludes old document versions | VERIFIED | `views.py` lines 1957-1961; `test_es_version_actual_filter` PASSES |
| 7 | Backend test covers archivo_url, filtering, and cross-employee isolation | VERIFIED | 4/4 tests in `test_documentos_digitales_onboarding.py` PASS |
| 8 | Frontend tests cover computeStatus all branches + subirDocumento endpoint | VERIFIED | 7/7 frontend tests PASS; all 5 SectionStatus branches covered |
| 9 | No regressions in pre-existing tests | VERIFIED | `test_empleado_update_v2.py` 5/5 PASS; `test_documentos_digitales_onboarding.py` new file 4/4 PASS |

**Score:** 9/9 truths verified

---

## Required Artifacts

| Artifact | Status | Details |
|----------|--------|---------|
| `back/api/v1/rrhh/serializers.py` | VERIFIED | `archivo_url = SerializerMethodField()` at line 1077; `"archivo_url"` in Meta.fields at line 1096; `get_archivo_url()` at line 1126 |
| `back/api/v1/rrhh/views.py` | VERIFIED | `es_version_actual` filter at lines 1957-1961; `archivo_url` in `subir_documento` response at line 2579; `archivo_url` in `subir_foto` at line 2471 |
| `front/src/App.tsx` | VERIFIED | `OnboardingGuard` component at line 102; `/onboarding` route wrapped at lines 179-183 |
| `front/src/features/onboarding/pages/OnboardingEmployeePage.tsx` | VERIFIED | `documentos-digitales` URL at line 27; `['legajo-docs', empleadoId]` queryKey at line 25 |
| `front/src/features/onboarding/services/onboardingDataService.ts` | VERIFIED | `getConstanciasTrabajo` uses `documentos-digitales` at line 64 |
| `front/src/features/onboarding/components/OnboardingTabFamiliar.tsx` | VERIFIED | `['legajo-docs', empleadoId]` invalidation at lines 153, 178, 236 |
| `front/src/features/onboarding/components/OnboardingTabAcademico.tsx` | VERIFIED | `['legajo-docs', empleadoId]` invalidation at lines 167, 244 |
| `front/src/features/onboarding/components/OnboardingTabLaboral.tsx` | VERIFIED | `['legajo-docs', empleadoId]` invalidation at lines 113, 238 |
| `front/src/features/onboarding/components/DocumentUploadZone.tsx` | VERIFIED | `['legajo-docs']` broad invalidation at line 44 |
| `back/tests/test_documentos_digitales_onboarding.py` | VERIFIED | 4 tests; all PASS |
| `front/src/features/onboarding/components/__tests__/OnboardingSectionStatus.test.tsx` | VERIFIED | 6 tests; all PASS |
| `front/src/features/onboarding/services/__tests__/onboardingUploadService.test.ts` | VERIFIED | 1 test; PASSES |

---

## Key Link Verification

| From | To | Via | Status | Details |
|------|----|-----|--------|---------|
| `DocumentosDigitalesSerializer` | `DocumentosDigitales.archivo` | `SerializerMethodField` + `build_absolute_uri(obj.archivo.url)` | WIRED | `get_archivo_url` at serializers.py line 1126; uses `self.context.get('request')` |
| `DocumentosDigitalesViewSet.get_queryset` | `es_version_actual` param | `filter(es_version_actual=True/False)` | WIRED | views.py lines 1957-1961 |
| `OnboardingGuard` in `App.tsx` | `Navigate to="/"` | `estado_onboarding === 'completado'` check | WIRED | App.tsx line 120-121 |
| `OnboardingEmployeePage.tsx` | `/api/v1/rrhh/documentos-digitales/` | `useQuery ['legajo-docs', empleadoId]` queryFn | WIRED | Line 27 with correct `params` object |
| `OnboardingTabFamiliar.tsx createMutation.onSuccess` | `['legajo-docs']` invalidation | `queryClient.invalidateQueries` | WIRED | Line 153 |
| `DocumentUploadZone.tsx handleUpload success` | `['legajo-docs']` invalidation | `queryClient.invalidateQueries` | WIRED | Line 44 |
| `test_documentos_digitales_onboarding.py` | `DocumentosDigitalesViewSet` | `onboarding_client.get('/api/v1/rrhh/documentos-digitales/')` | WIRED | 4 tests exercise the endpoint with onboarding employee token |

---

## Requirements Coverage

| Requirement | Source Plans | Description | Status | Evidence |
|-------------|-------------|-------------|--------|----------|
| ONBD-16 | 01.1-01, 01.1-02 | Personal tab shows/saves all required fields with AFP conditional | SATISFIED | Backend serializers verified in prior verification; 5/5 backend tests still PASS |
| ONBD-17 | 01.1-02, 01.1-06 | Section status panel PERSONAL/FAMILIAR/ACADEMICO/LABORAL with visual indicator | SATISFIED | `OnboardingSectionStatus.tsx` verified; now also covered by 6 Vitest unit tests |
| ONBD-18 | 01.1-03, 01.1-05 | Employee can click any uploaded document to view via preview modal | SATISFIED | `archivo_url` now returned by backend; "Ver documento" button guard `existingDoc.archivo_url` now has truthy value; 4 backend tests confirm field presence |
| ONBD-19 | 01.1-04 | "Agregar dependiente" dialog includes doc upload | SATISFIED | `OnboardingTabFamiliar.tsx` two-phase pattern verified in prior verification; `legajo-docs` invalidation now wired |
| ONBD-20 | 01.1-04 | Academico add-dialogs include doc upload | SATISFIED | `OnboardingTabAcademico.tsx` three dialogs verified; `legajo-docs` invalidation now wired |
| ONBD-21 | 01.1-04 | "Agregar experiencia laboral" dialog includes constancia upload | SATISFIED | `OnboardingTabLaboral.tsx` two-phase pattern verified; `legajo-docs` invalidation now wired |

No orphaned requirements: all 6 IDs (ONBD-16 through ONBD-21) appear in plan frontmatter and are mapped to Phase 1.1 in REQUIREMENTS.md.

---

## Anti-Pattern Scan

No anti-patterns found in gap-closure files:

- No `/api/v1/rrhh/legajo/` URL references remain in onboarding frontend (all replaced with `documentos-digitales`)
- No `TODO` / `FIXME` / `PLACEHOLDER` comments in modified files
- `OnboardingGuard` handles loading state with a visible spinner — no silent failures
- `get_archivo_url()` has a `None` guard (`if obj.archivo:`) — no AttributeError on docs without uploaded files
- `['legajo-docs']` broad invalidation in `DocumentUploadZone` is intentional — component doesn't have `empleadoId` in scope; behavior is correct

---

## Human Verification Required

The following items need browser-based validation to confirm the end-to-end behavior. They could not be verified programmatically.

### 1. Routing guard — completed employee redirect

**Test:** Log in as an employee whose `estado_onboarding` is `'completado'`. Navigate directly to `/onboarding` in the browser address bar.
**Expected:** Immediately redirected to `/` (dashboard) without seeing the onboarding form.
**Why human:** React Router navigation guards execute in the browser; programmatic verification cannot confirm the actual redirect behavior with the real auth/onboarding state.

### 2. "Ver documento" button — PDF preview modal opens

**Test:** Log in as an onboarding employee who previously uploaded a DNI document. Open the Personal tab or any tab where documents are shown. Click "Ver documento" on the DNI card.
**Expected:** A modal opens showing the PDF or image. The "Enviar documento" button is absent. A "Cerrar" button is present and closes the modal. The `archivo_url` displayed in the iframe/img src is an absolute URL (starts with `http://`).
**Why human:** Requires an actual uploaded file with a valid `archivo_url` returned by the real backend. The iframe rendering depends on browser PDF support and CORS headers.

### 3. Document list updates after upload without refresh

**Test:** As an onboarding employee, go to the Familiar tab. Add a new familiar record and attach a document in the dialog. Click "Guardar".
**Expected:** After the dialog closes and the success toast fires, the section status panel (FAMILIAR badge) updates to "En revisión" without manually refreshing the page.
**Why human:** React Query cache invalidation and re-fetch timing requires a live browser session to observe.

---

## Regression Check

| Test Suite | Before Gap Closure | After Gap Closure | Status |
|-----------|-------------------|-------------------|--------|
| `back/tests/test_empleado_update_v2.py` | 5/5 PASS | 5/5 PASS | No regression |
| `back/tests/test_documentos_digitales_onboarding.py` | (new) | 4/4 PASS | New green |
| `front/src/__tests__/OnboardingSectionStatus.test.tsx` | (new) | 6/6 PASS | New green |
| `front/src/__tests__/onboardingUploadService.test.ts` | (new) | 1/1 PASS | New green |

The 1 "failed suite" in the Vitest output (`tests/e2e/auth.test.js`) is the pre-existing Playwright e2e file that Vitest's glob picks up — it was failing before this phase and is unrelated to Phase 1.1 changes.

---

_Verified: 2026-03-15T14:40:00Z_
_Verifier: Claude (gsd-verifier)_
_Mode: Re-verification (6 gaps from UAT now closed)_
